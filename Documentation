
Semantic Versioning

How to run in command line:

Download the file and save it.

#if code is complete
Run "python3 semantic_verification.py 'input'" in the unix command line, where
'semantic_verification.py' is the path the the file and 'input' is the string to be checked.

#how to run code in a larger system

#to run my tests
in the section __main__, uncomment the "tests()" function and comment out the main function

#to run main file
uncomment the main() function and comment out the tests() function